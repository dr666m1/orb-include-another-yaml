version: 2.1

orbs:
  continuation: circleci/continuation@0.3

description: >
  This orb makes it easy to generate Dynamic Configuration.
  With this orb, You can use !import tag to include YAML file inside another.

display:
  # TODO edit home_url
  home_url: "https://www.example.com/docs"
  source_url: "https://github.com/dr666m1/orb-yaml-import"
